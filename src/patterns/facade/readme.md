the ApiClient interface provides the abstract client upload. 

To orchestrate this operation, create a Façade:

Façades can be used in cases where you have two APIs that need to be called one after the other